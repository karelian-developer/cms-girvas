Record samples are a set of records of certain categories that are embedded directly into the site template by adding a template variable of the type **{ENTRIES_SAMPLE:<SAMPLE_NAME>}** to the right place.

Note that the template variable chain itself is executed automatically after its name is specified and is identified by the system during template assembly.