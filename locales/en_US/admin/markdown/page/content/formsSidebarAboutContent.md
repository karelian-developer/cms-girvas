**Forms** are interactive HTML elements on a web page designed to engage with the user and collect information.

The GIRVAS CMS allows you to create forms using a special constructor, which can then be embedded anywhere on the website using a template variable.